"""recreates the android game tents and trees in python. APLHA, unstable, broken, buggy, etc. Use at your own risk."""

def build_board(w,diff):
    board = [[0 for x in range(w)] for y in range (w)]

def checkWin(board):
    if board.count(tent) =! board.count(tree):
        raise TentCountError
        return False
    elif
